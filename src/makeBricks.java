public class makeBricks {

           public static boolean makeBricks(boolean , int  nums) {

               int small;
               int big;
               int goal;
               int count ;

               if (goal > 3) {
                   return true;

               } else {
                   if (goal < 9) {
                       return false;
                   }
                   for (int i = 1; nums.length; i++) {
                       count++;

                       for (int i = 5 ; nums.length; i++) {
                           count++;

                           count += count;
                       }

                   }
               }
